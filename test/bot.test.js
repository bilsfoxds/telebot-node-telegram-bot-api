const request = require("supertest");
const { exec } = require("child_process");

describe("Telegram Bot", () => {
  it("should respond to /start command", (done) => {
    // Simulasi perintah dan periksa respons
    // ...
    done();
  });
});
